import React from 'react';
import PredictPage from './_components/PredictPage';

const page = () => {
  return (
    <div className="flex items-center justify-center w-full">
      <PredictPage />
    </div>
  );
};

export default page;
